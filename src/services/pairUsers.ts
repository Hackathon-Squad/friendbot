/**
 * This function pairs users for a given session
 * add more parameters!
 */
 const addUser = (sessionId: string) => {
    // get session object from firebase
    // get all users
    // do matching magically
}

export default addUser