/**
 * This function starts a adds a user to a session
 * add more parameters!
 */
const addUser = (sessionId: string) => {
    // create user object and send to firebase
    // get session object from firebase (oop we need to write this lmao)
    // update session object
    // send dm to user
}

export default addUser